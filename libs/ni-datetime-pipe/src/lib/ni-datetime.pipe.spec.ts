import { NiDatetimePipe } from './ni-datetime.pipe';

describe('NiDatetimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new NiDatetimePipe(null);
    expect(pipe).toBeTruthy();
  });
});
